# Copy to keystore.properties and fill in (this file is gitignored).
# Generate the keystore with: bash scripts/generate-release-keystore.sh
storeFile=filmgallery-release.keystore
storePassword=REPLACE_ME
keyAlias=filmgallery
keyPassword=REPLACE_ME
