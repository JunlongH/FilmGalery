// Runtime stub. @filmgallery/types is a type-only package; nothing is imported
// at runtime (consumers use `export type { ... }`). This file exists so the
// package `main` resolves if ever required.
module.exports = {};
